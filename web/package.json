{
  "name": "web",
  "description": "web REPL for grime",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "GOOS=js GOARCH=wasm go build -o web.wasm && env GOROOT=\"$(go env GOROOT)\" webpack"
  },
  "dependencies": {
    "buffer": "^5.2.1",
    "xterm": "^3.8.0"
  },
  "devDependencies": {
    "webpack": "^4.25.1",
    "webpack-cli": "^3.1.2"
  }
}
